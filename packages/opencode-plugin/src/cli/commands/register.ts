/**
 * GoopSpec CLI - Register Command
 * Registers the current project with the daemon.
 *
 * Stub — full implementation in Wave 3 (Task 3.2).
 */

import { warning } from "../theme.js";

export async function runRegister(_flags: Record<string, string | boolean>): Promise<void> {
  console.log(warning("'register' command is not yet implemented. Coming soon."));
}
